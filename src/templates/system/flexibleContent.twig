{% embed 'components/embed/flexibleContent' %}
    {# Add a {% block %} for each block in your component, for example: #}
    {% block copy %}
        <pre>{{ component | json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
    {% endblock %}
{% endembed %}
